{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "scripts": {
    "build:ui": "cd packages/ui && yarn && yarn build"
  },
  "workspaces": [
    "packages/*"
  ]
}
